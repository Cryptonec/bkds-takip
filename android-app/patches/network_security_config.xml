<?xml version="1.0" encoding="utf-8"?>
<!--
  Android Network Security Config
  Yerel ağ IP adreslerine HTTP ile bağlanmaya izin verir.
  APK kurulumundan sonra bu dosya otomatik olarak yerleştirilir (bkz. setup.sh).
-->
<network-security-config>
  <!-- Yerel ağ — HTTP'ye izin ver -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">192.168.0.0</domain>
    <domain includeSubdomains="true">192.168.1.0</domain>
    <domain includeSubdomains="true">10.0.0.0</domain>
    <domain includeSubdomains="true">10.0.1.0</domain>
    <domain includeSubdomains="true">172.16.0.0</domain>
    <domain includeSubdomains="true">localhost</domain>
  </domain-config>
  <!-- Dış internet — varsayılan (HTTPS) -->
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system"/>
    </trust-anchors>
  </base-config>
</network-security-config>
